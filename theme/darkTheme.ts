// theme/darkTheme.ts
// The dark theme now lives in ./index (it is the brand-default `theme`). This
// file re-exports it for back-compat with any existing imports.
export { darkTheme } from './index';
